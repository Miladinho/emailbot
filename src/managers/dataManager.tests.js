"use strict;"
const assert = require("assert")

describe('datamanager', () => {
    // it('should post a new campaign', () => {
    //     assert.equal(dataManager.postCampaign(data), {
    //         {}
    //     })
    // })
})